/** @type {import('next').NextConfig} */
const nextConfig = {
    // trailingSlash: true, // Si necesitas enlaces con barra final, como /about/
    // basePath: process.env.NODE_ENV === "production" ? "/milocai" : "",
    // assetPrefix: process.env.NODE_ENV === "production" ? "/milocai/" : "",
    output: 'export',
}

module.exports = nextConfig
